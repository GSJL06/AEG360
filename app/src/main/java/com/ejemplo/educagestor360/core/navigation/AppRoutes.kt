package com.ejemplo.educagestor360.core.navigation

// Objeto para definir las rutas de la aplicación de forma centralizada
object AppRoutes {
    const val LOGIN = "login_screen" // Ruta para la pantalla de Login
    // const val HOME = "home_screen" // Ejemplo: ruta para la pantalla principal (la usaremos después)
    // ... aquí agregarás más rutas a medida que crees más pantallas
}

